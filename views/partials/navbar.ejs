<nav class="fixed top-0 left-0 w-full h-20 z-50 glass-dark border-b border-white/10 flex items-center justify-between px-6 md:px-12">
    <div class="flex items-center gap-10">
      <a href="/home" class="text-xl font-light tracking-[8px] hover:opacity-70 transition-opacity whitespace-nowrap">AEUST RNG</a>
      <div class="hidden md:flex gap-8">
        <% const links = [
            { to: '/home', label: '首頁', icon: 'home' },
            { to: '/stores', label: '探索店家', icon: 'explore' },
            { to: '/about', label: '團隊與理念', icon: 'groups' }
        ]; %>
        
        <% links.forEach(link => { %>
            <a href="<%= link.to %>" 
               class="text-5sm uppercase tracking-[0.2em] font-bold flex items-center gap-2 hover:opacity-100 transition-opacity <%= path === link.to ? 'opacity-100 text-blue-400' : 'opacity-40' %>">
              <span class="material-symbols-rounded text-lg"><%= link.icon %></span>
              <%= link.label %>
            </a>
        <% }); %>
      </div>
    </div>
  
    <div class="flex items-center gap-3">
      <button onclick="toggleModal('dashboard-modal')" class="p-2 hover:bg-white/10 rounded-full transition-colors opacity-40 hover:opacity-100">
        <span class="material-symbols-rounded">bar_chart</span>
      </button>
      <button onclick="toggleModal('settings-modal')" class="p-2 hover:bg-white/10 rounded-full transition-colors opacity-40 hover:opacity-100">
        <span class="material-symbols-rounded">settings</span>
      </button>
      <div class="w-px h-4 bg-white/10 mx-2 hidden sm:block"></div>
      <a href="/login" class="px-5 py-2 rounded-full border border-white/20 text-sm font-bold tracking-widest hover:bg-white hover:text-black transition-all">
        LOGIN
      </a>
    </div>
</nav>